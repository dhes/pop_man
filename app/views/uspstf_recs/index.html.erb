<h1>USPSTF A and B Recommendations </h1>

<p>
  Of the <%= UspstfRec.where(in_scope: true).count%> USPSTF recommendations, <%= UspstfRec.where(in_scope: true).count%> are within our scope of practice, <%= UspstfRec.where(reminder_implemented: true).count%> have reminders and <%= UspstfRec.where(score_implemented: true).count%>
 have scores. 
</p><br>
 
<table class="table table-striped table-bordered table-condensed">
  <thead>
    <tr>
      <th>Topic</th>
      <th>Description</th>
      <th>Grade</th>
      <th>Release date</th>
      <th>In Scope</th>
      <th>Reminder Implemented</th>
      <th>Score Implemented</th>
    </tr>
  </thead>

  <tbody>
    <% @uspstf_recs.each do |uspstf_rec| %>
      <tr>
        <td><%= uspstf_rec.topic %></td>
        <td><%= uspstf_rec.description %></td>
        <td><center><%= uspstf_rec.grade %></center></td>
        <td><%= uspstf_rec.release_date %></td>
        <td><% if uspstf_rec.in_scope==true %><center>&#10003</center><% end %></td>
        <td><% if uspstf_rec.reminder_implemented==true %><center>&#10003</center><% end %></td>
        <td><% if uspstf_rec.score_implemented==true %><center>&#10003</center><% end %></td>
        <!--td><%= link_to 'Show', uspstf_rec %></td>
        <td><%= link_to 'Edit', edit_uspstf_rec_path(uspstf_rec) %></td-->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%#= link_to 'New Uspstf rec', new_uspstf_rec_path %>
